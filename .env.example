# Backend API Configuration
VITE_API_URL=http://localhost:5000
VITE_BACKEND_URL=http://localhost:5000

# Cloudinary Configuration
# Copy this to .env file and update with your actual values

VITE_CLOUDINARY_CLOUD_NAME=dit8gwqom
VITE_CLOUDINARY_UPLOAD_PRESET=ecopack

# NOTE: OpenAI API key is now configured on the BACKEND for security
# The AI features (Sustainability Chatbot and ESG Analyzer) now communicate
# with the backend API which handles OpenAI requests securely.
# DO NOT add VITE_OPENAI_API_KEY here - configure OPENAI_API_KEY in backend/.env instead

# Example:
# VITE_CLOUDINARY_CLOUD_NAME=ecopack-demo
# VITE_CLOUDINARY_UPLOAD_PRESET=ecopack_products
